{
  "name": "hidden-oak",
  "type": "module",
  "version": "0.1.0",
  "description": "Hidden Oak Banya static website, deployable to Cloudflare Pages.",
  "scripts": {
    "dev": "astro dev",
    "start": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "deploy": "astro build && wrangler pages deploy dist",
    "deploy:preview": "astro build && wrangler pages deploy dist --branch preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.3",
    "astro": "^7.0.9"
  }
}
