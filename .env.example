# Copy to .env for local development. Astro only exposes PUBLIC_ prefixed vars
# to the client. No secrets are required — the site is fully static and the
# Cal.com embed is hard-coded into the BookingEmbed component.
PUBLIC_SITE_URL=https://hidden-oak.pages.dev
